# uv configuration file
# Configure company internal PyPI mirror

# Primary mirror source (company internal PyPI)
#index-url = "https://pypi.company.com/simple"

# Trusted hosts (if company mirror uses self-signed certificate)
#allow-insecure-host = ["pypi.company.com"]

# Alternative mirror sources (optional, used when primary source fails)
# extra-index-url = [
#     "https://pypi.org/simple",  # Official source as backup
# ]
